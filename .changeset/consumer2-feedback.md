---
"@sethmakes/tokens": patch
"@sethmakes/css": patch
---

Consumer #2 feedback batch: state-driven checked styling on all choice controls (:checked / [aria-checked="true"] / --checked modifier — progressive-enhancement apps render checked state server-side); mk-fonts bin in tokens (blessed font-copy step for static pipelines); .mk-shell/.mk-shell__main (the appbar's document contract, shipped as code); theme-switcher recipe; complete class inventory in the css README + a docs cheatsheet page.
