/**
 * @sethmakes/components — intentionally empty.
 *
 * Components are added only when a real consuming project needs one
 * (see docs/VISION.md). The package exists to reserve its place in the
 * lockstep version group. Marked private until it has something to ship.
 */
export {};
